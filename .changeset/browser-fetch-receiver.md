---
"@turingfocus/chat-gateway-tfrobot": patch
---

Invoke a host-provided `fetch` without a foreign receiver so browsers stop
rejecting gateway requests with `Illegal invocation` before they leave the page.
