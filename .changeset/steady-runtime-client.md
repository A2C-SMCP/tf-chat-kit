---
"@turingfocus/chat-runtime": minor
---

Add the instance-scoped ChatClient with immutable snapshots, stable subscriptions, lossless subscribe-before-snapshot handoff, atomic history and realtime rebasing, incremental timeline indexing with a documented V1 scale envelope, text and run-pinned interrupt commands, race protection, observable listener and cleanup failures, and idempotent resource disposal.
